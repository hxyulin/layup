diagram "Keep supporting nodes together" layout=auto {
  note "The cache shares the worker's layer, even without an incoming edge."
  node api "API"
  node worker "Worker"
  node cache "Cache" same-layer=worker
  api -> worker
}
